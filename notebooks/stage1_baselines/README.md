Stage 1 prompting baselines: NLI encoders (DeBERTa-v3-large, RoBERTa-large,
zero-shot and few-shot probes) and decoder LLMs (zero-shot and few-shot
in-context learning), on CB1 and CB2. The decoder notebook is shown for
Llama-3.2-3B; the other five models use the identical notebook with only
the model name changed.
